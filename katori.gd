extends Area2D

class_name Katori
var empty = true

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass

func _on_chutney_scene_grabbed_katori() -> void:
	# duplicate the katori
	
	# this is the katori that the player 
	
	# the katori that is named katori needs to 
	pass
